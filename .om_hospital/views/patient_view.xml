<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" type="object" string="Confirm" states="draft"
                        confirm="Are you sure you want to confirm the patient?"
                        class="btn-primary" />
                    <button name="action_done" type="object" string="Mark As Done" states="confirm"
                        confirm="Are you sure you want to mark as done?"
                        class="btn-success" />
                    <button name="action_draft" type="object" string="Set To Draft" states="cancel"
                        confirm="Are you sure you want to reset to draft?"
                        class="btn-primary" />
                    <button name="action_cancel" type="object" string="Cancel"
                        states="draft,confirm,done"
                        confirm="Are you sure you want to cancel the patient?"
                        class="btn-danger" />

                    <button id="button_create_appointment"
                        name="%(om_hospital.action_create_appointment)d"
                        string="Create Appointment"
                        class="btn-primary"
                        type="action" />

                    <field name="state" widget="statusbar" statusbar_visible="draft,done,confirm" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_appointments" type="object" class="oe_stat_button"
                            icon="fa-calendar">
                            <div class="o_stat_info">
                                <field name="appointment_count" class="o_stat_value" />
                                <span class="o_stat_text">Appointments</span>
                            </div>
                        </button>
                    </div>

                    <field name="image" widget="image" class="oe_avatar" />
                    <div class="ose_title">
                        <h1>
                            <field name="reference" readonly="1" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="name" />
                            <field name="age" />
                            <field name="responsible_id" />
                        </group>
                        <group>
                            <field name="gender" invisible="context.get('hide_gender')" />
                            <field name="note" />
                            <field name="appointment_count" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Appointments" name="appointments">
                            <field name="appointment_ids" widget="one2many_list" readonly="1">
                                <tree editable="bottom">
                                    <field name="reference" />
                                    <field name="patient_id" />
                                    <field name="doctor_id" />
                                    <field name="age" optional="show" />
                                    <field name="gender" optional="show" />
                                    <field name="date_appointment" optional="show" />
                                    <field name="date_checkup" optional="show" />
                                    <field name="note" optional="show" />
                                    <field name="state" optional="show" />
                                </tree>
                                <form>
                                    <group>
                                        <group>
                                            <field name="patient_id" />
                                            <field name="age" />
                                            <field name="gender" />
                                        </group>
                                        <group>
                                            <field name="doctor_id" />
                                            <field name="date_appointment" />
                                            <field name="date_checkup" />
                                        </group>
                                    </group>
                                </form>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="activity_ids" />
                    <field name="message_ids" />
                </div>
            </form>
        </field>
    </record>

    <record id="view_patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree expand="1">
                <field name="reference" />
                <field name="name" />
                <field name="age" />
                <field name="gender" />
                <field name="note" />
                <field name="appointment_count" />
                <field name="state" />
            </tree>
        </field>
    </record>

    <record id="view_patient_kanban" model="ir.ui.view">
        <field name="name">hospital.patient.kanban</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <kanban default_order="reference asc">
                <!-- <field name="id" />
                <field name="name" />
                <field name="gender" />
                <field name="age" />
                <field name="note" /> -->
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <ul>
                                    <li>Name: <field name="name" /></li>
                                    <li>Age: <field name="age" /></li>
                                    <li>Gender: <field name="gender" /></li>
                                </ul>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_patient_search" model="ir.ui.view">
        <field name="name">hospital.patient.search</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <search string="Patients">
                <field name="name" />
                <field name="note" />
                
                <separator />
                <filter string="Male" name="male" domain="[('gender', '=', 'male')]" />
                <filter string="Female" name="female" domain="[('gender', '=', 'female')]" />
                <filter string="Other" name="other" domain="[('gender', '=', 'other')]" />

                <group expand="1" string="Group By">
                    <filter string="Gender" name="gender" context="{'group_by':'gender'}" />
                    <filter string="Patient Name" name="patient_id" context="{'group_by':'name'}" />
                    <filter string="Responsible" name="responsible_id"
                        context="{'group_by':'responsible_id'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="action_hospital_patient" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="context">{'search_default_responsible_id':1, 'search_default_female': 1}</field>
        <!-- <field name="context">{'search_default_gender': 1}</field> -->
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                Create your first patient!
            </p>
        </field>
    </record>

    <record id="action_create_appointments" model="ir.actions.server">
        <field name="name">Create Appointments</field>
        <!-- <field name="type">ir.actions.server</field> -->
        <field name="model_id" ref="om_hospital.model_hospital_patient" />
        <field name="binding_model_id" ref="model_hospital_patient" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            if record:
            action_values = env.ref('om_hospital.action_create_appointment').sudo().read()[0]
            action_values.update({'context' : env.context})
            action = action_values
        </field>
    </record>



</odoo>