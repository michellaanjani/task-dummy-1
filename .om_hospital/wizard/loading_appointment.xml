<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="view_loading_appointment_wizard_form" model="ir.ui.view">
        <field name="name">loading.appointment.wizard.form</field>
        <field name="model">loading.appointment.wizard</field>
        <field name="arch" type="xml">
            <form string="Loading Appointment">
                <footer>
                    <button name="action_loading_appointment" string="Load" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_loading_appointment" model="ir.actions.act_window">
        <field name="name">Loading Appointment</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">loading.appointment.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_loading_appointment_wizard_form"></field>
        <field name="target">new</field>
        <!-- 
            ⭐⭐⭐ Line 25: TARGET = NEW ⭐⭐⭐
            - "new" → BUKA SEBAGAI POP-UP / DIALOG
            - "current" → buka di halaman yang sama (replace)
            - "inline" → embed di halaman
            - "fullscreen" → buka fullscreen
            
            ✅ INI YANG MEMBUAT MUNCUL SEBAGAI POP-UP!
        -->
    </record>
</odoo>